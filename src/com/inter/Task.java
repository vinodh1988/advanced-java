package com.inter;

public interface Task {
   public void activity(String name);
}


//lambda expressions
//lambda expression comes in picture only if we have functional interface
//in place
//interface with one  method is what we call funtional interface

//lambda expression is a handy and alternate syntax for anonymous class

// but applicable if and only the anonymous class has one method unimplemented